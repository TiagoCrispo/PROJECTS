stCountdown()\n        else strengthStore.clearRestEnd()\n    }\n    private fun runRestCountdown() {\n        restTimerJob?.cancel()\n        restTimerJob = viewModelScope.launch {\n            while (true) {\n                val remainingMs = strengthStore.getRestEndEpochMs() - System.currentTimeMillis()\n                if (remainingMs <= 0L) break\n                val remaining = ((remainingMs + 999L) / 1_000L).toInt()\n                _strength.value = _strength.value.copy(restSecondsRemaining = remaining, restRunning = true)\n                delay(250)\n            }\n            strengthStore.clearRestEnd()\n            _strength.value = _strength.value.copy(restSecondsRemaining = 0, restRunning = false)\n        }\n    }\n    fun cancelRestTimer() {\n        restTimerJob?.cancel(); strengthStore.clearRestEnd()\n        _strength.value = _strength.value.copy(restSecondsRemaining = 0, restRunning = false)\n    }\n    fun setSplitMeters(meters: Int) { prefs.setSplitMeters(meters); _splitMeters.value = prefs.getSplitMeters(); _snackbar.value = "Splits: ${_splitMeters.value} m" }\n    fun getSplitMeters(): Int = _splitMeters.value\n'''
if old not in s: raise SystemExit('vm rest block anchor missing')
s=s.replace(old,new,1)
p.write_text(s)

# Training screen imports and UI enhancements
p=root/'app/src/main/java/com/fer/bandlab/ui/screens/TrainingScreen.kt'
s=p.read_text()
s=s.replace('import androidx.compose.foundation.clickable\n','import androidx.compose.foundation.clickable\nimport androidx.compose.foundation.Canvas\n')
s=s.replace('import androidx.compose.foundation.layout.fillMaxWidth\n','import androidx.compose.foundation.layout.fillMaxWidth\nimport androidx.compose.foundation.layout.height\n')
s=s.replace('import androidx.compose.ui.Alignment\n','import androidx.compose.ui.Alignment\nimport androidx.compose.ui.geometry.Offset\nimport androidx.compose.ui.geometry.Size\n')
# collect split
s=s.replace('''    val strengthStats by vm.strengthStats.collectAsStateWithLifecycle()\n''','''    val strengthStats by vm.strengthStats.collectAsStateWithLifecycle()\n    val splitMeters by vm.splitMeters.collectAsStateWithLifecycle()\n''')
# GPS buttons and status
old='''                    Row(horizontalArrangement = Arrangement.spacedBy(8.dp), modifier = Modifier.padding(top = 10.dp)) {\n                        FilledTonalButton(onClick = { vm.setSplitMeters(500) }, modifier = Modifier.weight(1f)) { Text("500 m") }\n                        FilledTonalButton(onClick = { vm.setSplitMeters(1000) }, modifier = Modifier.weight(1f)) { Text("1 km") }\n                    }\n                    Text("La ruta corta saltos >75 m y no une los extremos después de >10 s sin GPS.", style = MaterialTheme.typography.bodyMedium, color = TextSecondary, modifier = Modifier.padding(top = 8.dp))\n'''
new='''                    Text("Split activo: ${if (splitMeters == 500) "500 m" else "1 km"}", style = MaterialTheme.typography.bodyMedium, color = AccentBlue, modifier = Modifier.padding(top = 8.dp))\n                    Row(horizontalArrangement = Arrangement.spacedBy(8.dp), modifier = Modifier.padding(top = 8.dp)) {\n                        FilledTonalButton(onClick = { vm.setSplitMeters(500) }, modifier = Modifier.weight(1f)) { Text(if (splitMeters == 500) "✓ 500 m" else "500 m") }\n                        FilledTonalButton(onClick = { vm.setSplitMeters(1000) }, modifier = Modifier.weight(1f)) { Text(if (splitMeters == 1000) "✓ 1 km" else "1 km") }\n                    }\n                    Text("La ruta descarta saltos >75 m y, después de >10 s sin GPS, reinicia el tramo para no dibujar una línea falsa al recuperar señal.", style = MaterialTheme.typography.bodyMedium, color = TextSecondary, modifier = Modifier.padding(top = 8.dp))\n'''
if old not in s: raise SystemExit('training gps block missing')
s=s.replace(old,new,1)
# Replace sparkline and rest controls section
old='''                    Text("Volumen: ${"%.0f".format(strengthStats.totalVolumeKg)} kg · próxima: ${strengthStats.suggestedWeightKg?.let { "%.1f kg".format(it) } ?: "sin datos"}", style = MaterialTheme.typography.bodyMedium, color = TextSecondary, modifier = Modifier.padding(top = 10.dp))\n                    Text("Carga ${volumeSparkline(strengthStats.history.take(10).reversed().map { it.volume })}", style = MaterialTheme.typography.bodyLarge, modifier = Modifier.padding(top = 6.dp))\n                    OutlinedButton(onClick = { showSubstitutes = true }, modifier = Modif