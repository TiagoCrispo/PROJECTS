ier.fillMaxWidth().padding(top = 10.dp)) { Text("Sustituir ejercicio sin perder historial") }\n'''
new='''                    Text("Volumen histórico: ${"%.0f".format(strengthStats.totalVolumeKg)} kg · próxima carga: ${strengthStats.suggestedWeightKg?.let { "%.1f kg".format(it) } ?: "sin datos"}", style = MaterialTheme.typography.bodyMedium, color = TextSecondary, modifier = Modifier.padding(top = 10.dp))\n                    Text("Volumen · últimas series", style = MaterialTheme.typography.labelMedium, color = TextSecondary, modifier = Modifier.padding(top = 10.dp))\n                    VolumeBars(strengthStats.history.take(10).reversed().map { it.volume })\n                    if (strengthStats.history.isNotEmpty()) {\n                        Text("Historial reciente", style = MaterialTheme.typography.labelMedium, color = TextSecondary, modifier = Modifier.padding(top = 10.dp))\n                        strengthStats.history.take(4).forEach { set ->\n                            Text("${"%.1f".format(set.weightKg)} kg × ${set.reps} · e1RM ${"%.1f".format(set.e1rm)} kg", style = MaterialTheme.typography.bodyMedium, modifier = Modifier.padding(top = 3.dp))\n                        }\n                    }\n                    OutlinedButton(onClick = { showSubstitutes = true }, modifier = Modifier.fillMaxWidth().padding(top = 10.dp)) { Text("Sustituir ejercicio sin perder historial") }\n'''
if old not in s: raise SystemExit('strength sparkline block missing')
s=s.replace(old,new,1)
# insert rest preset before input row
anchor='''                    Row(horizontalArrangement = Arrangement.spacedBy(8.dp), modifier = Modifier.padding(top = 8.dp)) {\n                        OutlinedTextField(value = strengthWeight, onValueChange = { strengthWeight = it.replace(',', '.') }, label = { Text("kg") }, singleLine = true, keyboardOptions = KeyboardOptions(keyboardType = KeyboardType.Decimal), modifier = Modifier.weight(1f))\n'''
rep='''                    Text("Descanso por serie", style = MaterialTheme.typography.labelMedium, color = TextSecondary, modifier = Modifier.padding(top = 10.dp))\n                    Row(horizontalArrangement = Arrangement.spacedBy(6.dp), modifier = Modifier.padding(top = 6.dp)) {\n                        listOf(60, 90, 120).forEach { seconds ->\n                            FilledTonalButton(onClick = { vm.setRestPreset(seconds) }, modifier = Modifier.weight(1f)) {\n                                Text(if (strength.restPresetSeconds == seconds) "✓ ${seconds}s" else "${seconds}s")\n                            }\n                        }\n                    }\n                    Row(horizontalArrangement = Arrangement.spacedBy(8.dp), modifier = Modifier.padding(top = 8.dp)) {\n                        OutlinedTextField(value = strengthWeight, onValueChange = { strengthWeight = it.replace(',', '.') }, label = { Text("kg") }, singleLine = true, keyboardOptions = KeyboardOptions(keyboardType = KeyboardType.Decimal), modifier = Modifier.weight(1f))\n'''
if anchor not in s: raise SystemExit('rest preset anchor missing')
s=s.replace(anchor,rep,1)
# live split label + gps loss count
old='''                if (BuildConfig.FEATURE_STAGE >= 22 && live.splits.isNotEmpty()) {\n                    val last = live.splits.last()\n                    Text("Último split ${last.distanceMeters} m · ${formatDuration(last.splitMs)}", style = MaterialTheme.typography.bodyMedium, color = AccentBlue, modifier = Modifier.padding(top = 6.dp))\n                }\n'''
new='''                if (BuildConfig.FEATURE_STAGE >= 22 && live.splits.isNotEmpty()) {\n                    val last = live.splits.last()\n                    val previousDistance = live.splits.dropLast(1).lastOrNull()?.distanceMeters ?: 0\n                    val segmentMeters = last.distanceMeters - previousDistance\n                    Text("Último split ${segmentMeters} m · ${formatDuration(last.splitMs)}", style = MaterialTheme.typography.bodyMedium, color = AccentBlue, modifier = Modifier.padding(top = 6.dp))\n                }\n                if (BuildConfig.FEATURE_STAGE >= 22 && live.gpsLossCount > 0) {\n                    Text("Pérdidas GPS detectadas: ${live.gpsLossCount}", style = MaterialTheme.typography.bodyMedium, color = TextSecondary, modifier = Modifier.padding(top = 6.dp))\n                }\n'''
if old not in s: raise SystemExit('live split anchor missing')
s=s.replace(old,new,1)
# replace sparkline function with composable bars
start=s.inde