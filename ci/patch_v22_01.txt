rn null\n        val points = buildList {\n            add(0 to 0L)\n            workout.splits.sortedBy { it.distanceMeters }.forEach { add(it.distanceMeters to it.activeMs) }\n        }\n        var best: Long? = null\n        for (i in points.indices) {\n            for (j in i + 1 until points.size) {\n                val distance = points[j].first - points[i].first\n                if (distance < targetMeters) continue\n                if (distance > targetMeters) break\n                val elapsed = points[j].second - points[i].second\n                if (elapsed > 0L && (best == null || elapsed < best!!)) best = elapsed\n            }\n        }\n        return best\n    }\n}\n''')

# ViewModel split flow + persistent rest timer
p=root/'app/src/main/java/com/fer/bandlab/BandLabViewModel.kt'
s=p.read_text()
s=s.replace('''    private val _strength = MutableStateFlow(StrengthUiState())\n''','''    private val _strength = MutableStateFlow(StrengthUiState(restPresetSeconds = strengthStore.getRestPresetSeconds()))\n''')
anchor='''    private val _strengthStats = MutableStateFlow(strengthStore.stats(_strength.value.selectedExerciseId))\n    val strengthStats: StateFlow<StrengthExerciseStats> = _strengthStats.asStateFlow()\n    private var restTimerJob: kotlinx.coroutines.Job? = null\n'''
rep='''    private val _strengthStats = MutableStateFlow(strengthStore.stats(_strength.value.selectedExerciseId))\n    val strengthStats: StateFlow<StrengthExerciseStats> = _strengthStats.asStateFlow()\n    private val _splitMeters = MutableStateFlow(prefs.getSplitMeters())\n    val splitMeters: StateFlow<Int> = _splitMeters.asStateFlow()\n    private var restTimerJob: kotlinx.coroutines.Job? = null\n'''
if anchor not in s: raise SystemExit('vm split anchor missing')
s=s.replace(anchor,rep,1)
s=s.replace('''        if (address != null && name != null) viewModelScope.launch { delay(450); bandManager.connect(name, address) }\n''','''        if (address != null && name != null) viewModelScope.launch { delay(450); bandManager.connect(name, address) }\n        resumeRestTimerIfNeeded()\n''')
old='''    fun addStrengthSet(weightKg: Double, reps: Int, restSeconds: Int = 90) {\n        runCatching { strengthStore.addSet(_strength.value.selectedExerciseId, weightKg, reps) }\n            .onSuccess {\n                _strengthStats.value = strengthStore.stats(_strength.value.selectedExerciseId)\n                _strength.value = _strength.value.copy(lastMessage = "Serie guardada · ${weightKg} kg × $reps")\n                startRestTimer(restSeconds)\n            }.onFailure { _snackbar.value = it.message ?: "No pude guardar la serie" }\n    }\n    private fun startRestTimer(seconds: Int) {\n        restTimerJob?.cancel()\n        restTimerJob = viewModelScope.launch {\n            for (s in seconds downTo 0) {\n                _strength.value = _strength.value.copy(restSecondsRemaining = s, restRunning = s > 0)\n                if (s > 0) delay(1_000)\n            }\n        }\n    }\n    fun cancelRestTimer() { restTimerJob?.cancel(); _strength.value = _strength.value.copy(restSecondsRemaining = 0, restRunning = false) }\n    fun setSplitMeters(meters: Int) { prefs.setSplitMeters(meters); _snackbar.value = "Splits: ${meters} m" }\n    fun getSplitMeters(): Int = prefs.getSplitMeters()\n'''
new='''    fun addStrengthSet(weightKg: Double, reps: Int, restSeconds: Int = _strength.value.restPresetSeconds) {\n        runCatching { strengthStore.addSet(_strength.value.selectedExerciseId, weightKg, reps) }\n            .onSuccess {\n                _strengthStats.value = strengthStore.stats(_strength.value.selectedExerciseId)\n                _strength.value = _strength.value.copy(lastMessage = "Serie guardada · ${weightKg} kg × $reps")\n                startRestTimer(restSeconds)\n                refreshTodayDashboard()\n            }.onFailure { _snackbar.value = it.message ?: "No pude guardar la serie" }\n    }\n    fun setRestPreset(seconds: Int) {\n        strengthStore.setRestPresetSeconds(seconds)\n        _strength.value = _strength.value.copy(restPresetSeconds = strengthStore.getRestPresetSeconds())\n    }\n    private fun startRestTimer(seconds: Int) {\n        val safe = seconds.coerceIn(1, 600)\n        strengthStore.setRestEndEpochMs(System.currentTimeMillis() + safe * 1_000L)\n        runRestCountdown()\n    }\n    private fun resumeRestTimerIfNeeded() {\n        if (strengthStore.getRestEndEpochMs() > System.currentTimeMillis()) runRe