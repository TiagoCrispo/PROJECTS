# FurnitureShot AI v0.8.5-reference-match2

Second controlled SOURCE-vs-QUALITY-REFERENCE iteration.

Goals encoded from the supplied reference instead of relying on prompt text:
- preserve exact source geometry and object identity;
- neutral catalog wall/floor backdrop with a soft transition;
- more breathing room in composition without any digital upscaling;
- reduce washed/orange highlight chroma while keeping real source wood hue;
- add luma-only source-anchored microcontrast to visible wood texture;
- keep support-derived contact shadow conservative;
- keep v0.8.1 runtime hotfix and v0.8.3 clean fullscreen viewer unchanged.

MAX ONLY. No profiles, no generative object reconstruction, no warp, no oval/drop shadow.
