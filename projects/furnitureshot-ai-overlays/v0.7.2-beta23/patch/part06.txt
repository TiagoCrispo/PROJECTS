         val blurGuard = if (quality.blurScore < 0.24f) 0.82f else 1f
-        val gainGuard = confidenceGuard * noiseGuard * blurGuard
+        val strength = strengthMultiplier.coerceIn(0.45f, 1.15f)
+        val gainGuard = confidenceGuard * noiseGuard * blurGuard * strength
 
         return when (material) {
             SurfaceMaterial.WOOD -> EnhancementParameters(
diff -ruN beta22base/furnitureshot-ai/app/src/main/java/com/tiagocrispo/furnitureshot/processing/PhotometricIntegrator.kt beta23src/furnitureshot-ai/app/src/main/java/com/tiagocrispo/furnitureshot/processing/PhotometricIntegrator.kt
--- beta22base/furnitureshot-ai/app/src/main/java/com/tiagocrispo/furnitureshot/processing/PhotometricIntegrator.kt	2026-08-17 22:56:04.000000000 +0000
+++ beta23src/furnitureshot-ai/app/src/main/java/com/tiagocrispo/furnitureshot/processing/PhotometricIntegrator.kt	2026-08-18 00:45:23.661195893 +0000
@@ -42,6 +42,7 @@
         source: Bitmap,
         alpha: FloatArray,
         inputQuality: InputQualityReport,
+        strengthMultiplier: Float = 1f,
     ): PhotometricIntegrationResult {
         val width = source.width
         val height = source.height
@@ -66,6 +67,7 @@
         }
 
         val out = pixels.clone()
+        val strength = strengthMultiplier.coerceIn(0.35f, 1.05f)
         val highlightGuard = (1f - inputQuality.highlightClipRatio * 5f).coerceIn(0.55f, 1f)
         val shadowGuard = (1f - inputQuality.shadowClipRatio * 4f).coerceIn(0.65f, 1f)
 
@@ -82,12 +84,12 @@
             analysis.p10Luminance < 0.08f -> 0.040f
             analysis.p10Luminance < 0.15f -> 0.028f
             else -> 0.014f
-        } * shadowGuard
+        } * shadowGuard * strength
         val highlightCompression = when {
             analysis.p90Luminance > 0.93f -> 0.050f
             analysis.p90Luminance > 0.84f -> 0.032f
             else -> 0.014f
-        } * highlightGuard
+        } * highlightGuard * strength
 
         for (i in pixels.indices) {
             val a = alpha[i].coerceIn(0f, 1f)
@@ -108,7 +110,7 @@
 
             // Very small S-curve: enough to retain plane separation but not enough
             // to create an HDR/clarity appearance.
-            val midContrast = 1.012f
+            val midContrast = 1f + 0.012f * strength
             mappedLum = ((mappedLum - 0.5f) * midContrast + 0.5f).coerceIn(0f, 1f)
 
             val lumaScale = (mappedLum / originalLum).coerceIn(0.93f, 1.07f)
@@ -130,7 +132,7 @@
             }
 
             // Near matte edges, retain more of the untouched product color.
-            val processingBlend = (0.82f * a).coerceIn(0f, 0.82f)
+            val processingBlend = ((0.50f + 0.32f * strength) * a).coerceIn(0f, 0.82f)
             r = sr * (1f - processingBlend) + r * processingBlend
             g = sg * (1f - processingBlend) + g * processingBlend
             b = sb * (1f - processingBlend) + b * processingBlend
diff -ruN beta22base/furnitureshot-ai/app/src/main/java/com/tiagocrispo/furnitureshot/processing/PromptPolicy.kt beta23src/furnitureshot-ai/app/src/main/java/com/tiagocrispo/furnitureshot/processing/PromptPolicy.kt
--- beta22base/furnitureshot-ai/app/src/main/java/com/tiagocrispo/furnitureshot/processing/PromptPolicy.kt	2026-08-17 22:34:07.000000000 +0000
+++ beta23src/furnitureshot-ai/app/src/main/java/com/tiagocrispo/furnitureshot/processing/PromptPolicy.kt	2026-08-18 00:42:40.925684642 +0000
@@ -1,15 +1,56 @@
 package com.tiagocrispo.furnitureshot.processing
 
 import com.tiagocrispo.furnitureshot.model.ProcessSettings
+import com.tiagocrispo.furnitureshot.model.QualityProfile
 
 object PromptPolicy {
-    fun automaticSettings(): ProcessSettings = ProcessSettings(
-        brightness = 0.012f,
-        contrast = 1.028f,
-        warmth = 0.006f,
-        shadowStrength = 0f,
-        saturation = 1.01f,
-        detailStrength = 0.07f,
-        localContrastAmount = 0.06f,
-    )
+    fun automaticSettings(profile: QualityProfile = QualityProfile.QUALITY_MAX): ProcessSettings = when (profile) {
+        QualityProfile.FAST -> ProcessSettings(
+            brightness = 0.006f,
+            contrast = 1.012f,
+            warmth = 0.003f,
+            shadowStrength = 0f,
+            saturation = 1.004f,
+            detailStrength = 0.035f,
+            localContrastAmount = 0.025f,
+            qualityProfile = profile,
+            structureLockStrength = 0.90f,
+            textureStrength = 0.55f,
+            lightingStrength = 0.45f,
+            detailRecoveryStrength = 0.55f,
+            objectPreservationStrength = 0.95f,
+        )
+        QualityProfile.BALANCED -> ProcessSettings(
+            brightness = 0.008f,
+            contrast = 1.018f,
+            warmth = 0.004f,
+            shadowStrength = 0f,
+            saturation = 1.006f,
+            detailStrength = 0.045f,
+            localContrastAmount = 0.035f,
+            qualityProfile = profile,
+            structureLockStrength = 0.94f,
+            textureStrength = 0.70f,
+            lightingStrength = 0.56f,
+            detailRecoveryStrength = 0.76f,
+            objectPreservationStrength = 0.97f,
+        )
+        QualityProfile.QUALITY_MAX -> ProcessSettings(
+            // QUALITY MAX intentionally starts more conservatively than beta22. The
+            // later material/photometric stages are mask-aware and have stronger QA.
+            brightness = 0.002f,
+            contrast = 1.006f,
+            warmth = 0.001f,
+            shadowStrength = 0f,
+            saturation = 1.002f,
+            detailStrength = 0.025f,
+            localContrastAmount = 0.018f,
+            qualityProfile = profile,
+            structureLockStrength = 0.985f,
+            textureStrength = 0.88f,
+            lightingStrength = 0.70f,
+            detailRecoveryStrength = 0.94f,
+            objectPreservationStrength = 0.992f,
+        )
+    }
 }
diff -ruN beta22base/furnitureshot-ai/app/src/main/java/com/tiagocrispo/furnitureshot/processing/StructureLockController.kt beta23src/furnitureshot-ai/app/src/main/java/com/tiagocrispo/furnitureshot/processing/StructureLockController.kt
--- beta22base/furnitureshot-ai/app/src/main/java/com/tiagocrispo/furnitureshot/processing/StructureLockController.kt	1970-01-01 00:00:00.000000000 +0000
+++ beta23src/furnitureshot-ai/app/src/main/java/com/tiagocrispo/furnitureshot/processing/StructureLockController.kt	2026-08-18 00:43:09.298554637 +0000
@@ -0,0 +1,121 @@
+package com.tiagocrispo.furnitureshot.processing
+
+import android.graphics.Bitmap
+import android.graphics.Color
+import kotlin.math.abs
+import kotlin.math.max
+import kotlin.math.roundToInt
+
+/**
+ * Pixel-level STRUCTURE LOCK.
+ *
+ * The reference photograph is the source of truth. Strong structural edges and
+ * alpha-boundaries are pulled back toward the original RGB instead of attempting
+ * to repair geometry by warping or generation. This lets tone/material passes act
+ * inside surfaces while keeping legs, slats, tabletop edges and joints anchored.
+ */
+data class StructureProtectionReport(
+    val protectedPixelRatio: Float,
+    val meanProtection: Float,
+    val geometry: StructureGeometryReport,
+    val accepted: Boolean,
+)
+
+data class StructureProtectionResult(
+    val bitmap: Bitmap,
+    val report: StructureProtectionReport,
+)
+
+object StructureLockController {
+    fun protect(
+        reference: Bitmap,
+        candidate: Bitmap,
+        alpha: FloatArray,
